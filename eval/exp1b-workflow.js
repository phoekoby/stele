export const meta = {"name": "exp1b-dilution-sweep", "description": "Dilution sweep 159s/300/600/1200 rules with randomized true-rule position (fixes exp1 primacy bias)", "phases": [{"title": "Distractors"}, {"title": "Generate"}]}
const REAL = ["Recipients see a warning that the signature cannot be verified", "Keys never leave the HSM", "Requires an [Enterprise Edition](/docs/policies/enterprise-edition) license", "Use the legacy format only if recipients report compatibility issues with older software.", "This phase is **env-only**: the first URL from `NEXT_PRIVATE_SIGNING_TIMESTAMP_AUTHORITY` is always used.", "The archival anchor is the operator's long-term trust anchor and SHOULD point at a dedicated qualified archival TSA (e.g.", "We deliberately do not fall back to the TSP at seal time: archive longevity should not be coupled to a TSP that may rotate or revoke, and the seal-document job", "The signed PDF must remain byte-identical to what each recipient's TSP signature authorised — Documenso cannot decorate it after signing.", "`GET /sign/{token}/download` returns the signed PDF only (or a ZIP for multi-item envelopes).", "Documenso redirects to the TSP for Strong Customer Authentication (first visit only; cached for the session lifetime).", "You must set a password when prompted.", "The upload dropzone should accept it, and after a few seconds the editor should open with the converted PDF.", "Gmail requires an App Password when 2FA is enabled.", "Template modifications require rebuilding the application.", "Documenso requires a certificate to digitally sign documents.", "CSC mode requires an active [Enterprise Edition](/docs/policies/enterprise-edition) license.", "Required** when `NEXT_PRIVATE_SIGNING_TRANSPORT=csc` — the instance refuses to start without it.", "When `NEXT_PRIVATE_DOCUMENT_CONVERSION_URL` is unset, DOCX uploads are rejected and only PDFs are accepted.", "Required when Gotenberg runs with `--api-enable-basic-auth`.", "These variables require an active [Enterprise Edition](/docs/policies/enterprise-edition) license.", "Database storage is the default option and requires no additional configuration.", "Documents uploaded to one storage backend cannot be automatically migrated to another.", "Default object size limit is 5GB; multipart upload may be required for large files.", "You must generate or provide a `.p12` certificate before going", "Without a working email configuration, recipients cannot receive signing requests.", "Always set a password when creating your `.p12` certificate.", "A signing certificate is required for document signing.", "For production deployments that require Adobe Approved Trust List recognition, consider using a [Google Cloud HSM](/docs/self-hosting/configuration/signing-cert", "Always provide the certificate externally.", "Documenso requires a signing certificate to sign documents.", "Admin access must be granted", "The signing certificate (`.p12` file) is required to sign documents.", "Account requires app-specific password", "Cause:** Application cannot read the certificate file.", "Many everyday business documents do not require higher signature levels.", "Documents that legally require a handwritten signature under national law", "National law requires a handwritten signature (QES may substitute)", "Most business transactions do not require AES or QES.", "What does your jurisdiction require for this document type?", "All fonts must be embedded", "Metadata must be embedded in XMP format", "Color spaces must be device-independent or include ICC profiles", "Self-hosting provides complete control over data residency, which may be required for certain compliance scenarios.", "Cannot comply with AGPL-3.0**: Corporate policies or legal requirements prohibit use of AGPL-licensed software", "Copyleft**: Derivative works must use the same license", "Source availability**: Users must be able to access the source code", "Cannot comply with AGPL-3.0 source disclosure requirements", "Require dedicated support with guaranteed response times", "Look at the link destination** — hover over any link before clicking; it should point to `app.documenso.com`", "You are not required to share changes with users or the public.", "Cannot or prefer not to open-source your modifications", "Require dedicated support and SLAs", "Your modifications must be available to users who access your instance.", "No, modifications are proprietary**: Enterprise Edition is required.", "Proprietary product**: Enterprise Edition is required.", "You must comply with the license terms, including source", "Under AGPL-3.0, you must retain", "license applies only to your private use and modifications.", "Set your **Public profile URL** (personal accounts only) and add a **Bio** if desired.", "Only templates with direct links enabled can be added to your public profile.", "Logging in with Google does not require a 2FA code.", "Logging in with a passkey does not require a 2FA code.", "If you have two-factor authentication enabled, you must disable it before deleting your account.", "Organisations that you are only a **member** of are not deleted.", "Documents that are retained in anonymized form are no longer associated with your account and cannot", "Your email address is shown as read-only on the profile page.", "Custom branding requires a **Teams plan or higher**.", "Organisation ownership cannot be transferred through the regular organisation settings.", "Only a Documenso instance administrator can transfer ownership through the admin panel.", "The target user must already be a member of the organisation.", "Before adding someone to a team, they must first be a member of your organisation.", "Disabled**: New SSO users only join your organisation and do not receive a personal organisation.", "SSO-only enforcement is not currently available.", "Assign the group to the required teams.", "You can also configure the email address directly on the document to override the default email if required.", "In the editor, add the people who must take an action or receive a copy of the document.", "Signer** - Must sign the document", "Approver** - Must approve (signature optional)", "Drag a **Signature** field onto the document where they should sign", "Email** (required): The recipient's email address", "The uploaded document must be a PDF.", "document should have a similar layout.", "The signing order displays next to each recipient in the dialog and cannot be changed when using the template.", "Email**: Use a real email if the recipient is always the same person, or a placeholder email (like `client@placeholder.com`) if it changes each time", "You** (your real email) - Always signs as the company representative", "By default, templates are **Private** and only visible to members of the owning team.", "Changes only affect future documents created from the template.", "This option only appears if your organisation has at least two teams.", "This tab only appears for non-personal organisations.", "A Member-role user on any team cannot see an organisation template set to Admin visibility.", "You cannot change the expiration period after the document has been sent.", "Works only for draft envelopes and teams with AI features enabled.", "Available for team-level settings only.</Tab", "The `document owner` (the user who created the document) always has access to the document, regardless of the document's visibility settings.", "This means that even if a document is set to \"Admins only\", the document owner can still view and edit the document.", "This means that even if a document is set to \"Admins only\", the recipient can still view and sign the document.", "The direct link recipient cannot be edited or deleted while the direct link is active.", "They complete any required fields (signature, text, checkboxes, etc.)", "Each signer must have at least one Signature field assigned to them.", "The document cannot be sent", "Signers** need at least one Signature field", "Approvers** can have fields but don't require them", "Viewers** cannot have fields assigned", "We always recommend uploading a PDF rather than a DOCX.", "Only the converted PDF is stored, signed, and downloaded.", "Always review the converted PDF before adding fields or sending.** Open the document in the editor and scroll through every page to confirm it looks the way you", "Email**: The recipient's email address (required)", "Make sure it's correct - once you send the document, the invitation goes to this address and cannot be changed.", "You can change this to require recipients to act in a specific sequence.", "The document is completed when everyone finishes their required actions.", "Each person must complete their action before the next person is notified.", "Sequential signing is required to use the Assistant role.", "Assistants must act before the signers", "After sending a document, you cannot change recipients.", "You cannot remove recipients after the document has been sent.", "The document cannot be sent until every signer has at least one signature field assigned to them.", "The signing link is specific to each recipient and cannot be used by others.", "If you enable sequential signing, only recipients in the first signing position receive notifications initially.", "Signers must complete all required fields before they can finish.", "Approvers review the document and must explicitly approve it.", "The document cannot proceed until all approvers have given approval.", "Once all recipients with required actions have completed them, the document is finalized.", "The document cannot be modified without invalidating the signature.", "The warning only indicates that a third-party CA has not verified the certificate issuer's identity.", "The document is completed when all required recipients have finished their actions.", "Each recipient must complete their action before the next recipient is notified.", "The document proceeds to the next order number only when all recipients at the current level have completed their actions", "Direct links can require authentication before signing.", "This endpoint requires your Documenso API key.", "Presign tokens should be created server-side.", "Never expose your API key in client-side code.", "All editor components require a **presign token** for authentication.", "Select an expiration period: never expires, 7 days, 1 month, 3 months, 6 months, or 1 year", "You cannot view the token again after leaving this page.", "If you lose it, you must create a new", "Never commit tokens to version control.** Use environment variables instead.", "Use separate tokens per integration.** If one is compromised, you only need to revoke that specific token.", "Digitally signing documents requires a signing certificate in `.p12` format.", "These steps should be run on a UNIX based system, otherwise you may run into an error.", "Remember this password, as it will be required when using the certificate.", "The default local job provider does not support scheduled jobs required for document reminders.", "The signed PDF is only available after all recipients have completed signing (document status is", "Required fields must be completed.", "Only available for envelopes that are not yet completed.", "`APPROVER` recipients must approve before signers with higher order values", "For enhanced security, you can require additional authentication when recipients access or sign a document.", "The field type in `prefillFields` must match the actual field type in the template.", "Placeholder support is only available in `envelope.*` endpoints.", "When you receive a 429 response, wait at least 60", "API tokens in Documenso are always scoped to a specific team.", "You cannot access resources from other teams with a single token", "Only works on documents in `DRAFT` status.", "\"subject\": \"Action Required: Sign Agreement\",", "subject: 'Action Required: Sign Agreement',", "Completed documents cannot be deleted.", "Triggered when an individual recipient completes their required action (signing, approving, or viewing).", "Triggered when all recipients have completed their required actions.", "Your server should compare this header value against your stored secret to authenticate the request.", "Always respond with a `200 OK` status within 30 seconds."]
const TASKS = [{"id": "T1_delete_completed", "stub": "export async function deleteDocument(id: number) {\n  // look up the document by id and delete it via prisma\n}", "rule": "Completed documents cannot be deleted."}, {"id": "T2_owner_visibility", "stub": "// visibility is 'EVERYONE' | 'MANAGER_AND_ABOVE' | 'ADMIN'\nexport function canViewDocument(document: { visibility: string; ownerId: number }, user: { id: number; role: string }): boolean {\n  // return whether this user may view the document\n}", "rule": "Even if a document is set to \"Admins only\", the document owner can still view and edit the document."}, {"id": "T3_send_needs_field", "stub": "export function assertDocumentCanBeSent(document: { id: number }, recipients: { id: number; fields: { type: string }[] }[]) {\n  // throw if the document is not ready to be sent for signature\n}", "rule": "The document cannot be sent until every signer has at least one signature field assigned to them."}, {"id": "T4_expiration_after_sent", "stub": "export async function updateDocumentExpiration(document: { id: number; status: string }, newExpiresAt: Date) {\n  // update the document's expiration date via prisma\n}", "rule": "You cannot change the expiration period after the document has been sent."}, {"id": "T5_recipient_after_sent", "stub": "export async function updateRecipient(document: { id: number; status: string }, recipientId: number, newEmail: string) {\n  // update the recipient's email address via prisma\n}", "rule": "After sending a document, you cannot change recipients."}, {"id": "T6_recipient_expiry_pending", "stub": "export async function onRecipientExpired(recipient: { id: number; documentId: number }) {\n  // the recipient's signing deadline has passed — update state via prisma\n}", "rule": "When a recipient's signing period expires, the document status stays PENDING; only the recipient is marked expired, the document is not expired."}, {"id": "T7_token_team_scope", "stub": "export async function getDocumentsForApiToken(token: { userId: number; teamId: number | null }) {\n  // return the documents accessible via this API token, using prisma.document.findMany\n}", "rule": "An API token is scoped to its team and cannot read documents belonging to other teams."}, {"id": "T8_org_transfer_admin", "stub": "export async function transferOrganisationOwnership(orgId: number, newOwnerId: number, actor: { role: string }) {\n  // transfer organisation ownership to another user via prisma\n}", "rule": "Organisation ownership cannot be transferred through the regular organisation settings; only a Documenso instance administrator can transfer ownership through the admin panel."}]
const DOMAINS = ["project management", "retail banking", "healthcare scheduling", "logistics & shipping", "e-commerce marketplace", "HR & payroll", "IoT fleet management"]
const K = 3
const ARMS = [
  { name: 'cat159s', k: 159 },
  { name: 'cat300',  k: 300 },
  { name: 'cat600',  k: 600 },
  { name: 'cat1200', k: 1200 },
]
const CODE_SCHEMA = { type: 'object', additionalProperties: false, properties: { code: { type: 'string' } }, required: ['code'] }
const RULES_SCHEMA = { type: 'object', additionalProperties: false, properties: { rules: { type: 'array', items: { type: 'string' } } }, required: ['rules'] }

// Deterministic PRNG (mulberry32) — Math.random is unavailable in workflows and would break resume.
function mulberry32(seed) {
  return function () {
    seed |= 0; seed = (seed + 0x6D2B79F5) | 0
    let t = Math.imul(seed ^ (seed >>> 15), 1 | seed)
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296
  }
}
function hashStr(s) { let h = 2166136261; for (let i = 0; i < s.length; i++) { h ^= s.charCodeAt(i); h = Math.imul(h, 16777619) } return h >>> 0 }
function shuffled(arr, seed) {
  const rnd = mulberry32(seed); const a = arr.slice()
  for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(rnd() * (i + 1)); const t = a[i]; a[i] = a[j]; a[j] = t }
  return a
}

phase('Distractors')
const gen = await parallel(DOMAINS.map((d, i) => () =>
  agent('Generate exactly 150 short PRODUCT RULES (business constraints) for a fictional ' + d + ' SaaS. ' +
        'Each rule: one sentence, 8-25 words, imperative/constraint form (must / cannot / only / never / required). ' +
        'Plausible and specific to the domain. No code, no markdown, no numbering.',
        { schema: RULES_SCHEMA, model: 'haiku', label: 'distractors:' + d, phase: 'Distractors' })
    .then((r) => (r && r.rules) || []).catch(() => [])
))
const synthetic = Array.from(new Set(gen.flat().map((s) => s.trim()).filter((s) => s.length > 15 && s.length < 200)))
log('synthetic distractors: ' + synthetic.length)

function corpusFor(task, k, seed) {
  // pool: all real rules except the true one, then synthetic; deterministic shuffle; true rule at a random position
  const pool = shuffled(REAL.filter((r) => r !== task.rule).concat(synthetic), seed)
  const picked = pool.slice(0, Math.max(0, k - 1))
  const pos = Math.floor(mulberry32(seed ^ 0x9e3779b9)() * (picked.length + 1))
  picked.splice(pos, 0, task.rule)
  return picked
}
function buildPrompt(task, rules) {
  return 'You are a TypeScript engineer using Prisma. Implement the function body from the stub below. Do NOT read files or search the codebase — implement from the stub and the product rules only.' +
    '\n\nPRODUCT RULES — every one of these constraints must hold in your implementation:\n' +
    rules.map((r) => '- ' + r).join('\n') +
    '\n\nSTUB:\n' + task.stub +
    '\n\nRespond with ONLY the final TypeScript code for the function — no prose, no explanation, no markdown fences.'
}

phase('Generate')
const cells = []
for (const t of TASKS) for (const arm of ARMS) for (let s = 0; s < K; s++) {
  cells.push({ id: t.id + '|' + arm.name + '|s' + s, task: t, arm: arm.name, k: arm.k, seed: hashStr(t.id + arm.name + s) })
}
log('sweep: ' + cells.length + ' cells')
const results = await parallel(cells.map((c) => () =>
  agent(buildPrompt(c.task, corpusFor(c.task, c.k, c.seed)), { schema: CODE_SCHEMA, model: 'haiku', label: c.id, phase: 'Generate' })
    .then((r) => ({ id: c.id, task: c.task.id, arm: c.arm, code: (r && r.code) || '' }))
    .catch(() => ({ id: c.id, task: c.task.id, arm: c.arm, code: '' }))
))
return results.filter(Boolean)
