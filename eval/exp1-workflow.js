export const meta = {"name": "exp1-rule-dilution", "description": "Rule-compliance vs #rules-in-context: scoped (stele) vs prompt-stuffing (cat-N) vs plain, Haiku", "phases": [{"title": "Generate"}]}
const CORPUS = ["Logging in with a passkey does not require a 2FA code.", "A Member-role user on any team cannot see an organisation template set to Admin visibility.", "Signers must complete all required fields before they can finish.", "Self-hosting provides complete control over data residency, which may be required for certain compliance scenarios.", "Required fields must be completed.", "By default, templates are **Private** and only visible to members of the owning team.", "Select an expiration period: never expires, 7 days, 1 month, 3 months, 6 months, or 1 year", "The document proceeds to the next order number only when all recipients at the current level have completed their actions", "Never commit tokens to version control.** Use environment variables instead.", "Custom branding requires a **Teams plan or higher**.", "Admin access must be granted", "The document cannot be modified without invalidating the signature.", "You cannot access resources from other teams with a single token", "Required when Gotenberg runs with `--api-enable-basic-auth`.", "The direct link recipient cannot be edited or deleted while the direct link is active.", "Approvers review the document and must explicitly approve it.", "Recipients see a warning that the signature cannot be verified", "When `NEXT_PRIVATE_DOCUMENT_CONVERSION_URL` is unset, DOCX uploads are rejected and only PDFs are accepted.", "Your modifications must be available to users who access your instance.", "Once all recipients with required actions have completed them, the document is finalized.", "What does your jurisdiction require for this document type?", "SSO-only enforcement is not currently available.", "Never expose your API key in client-side code.", "This means that even if a document is set to \"Admins only\", the document owner can still view and edit the document.", "Requires an [Enterprise Edition](/docs/policies/enterprise-edition) license", "These variables require an active [Enterprise Edition](/docs/policies/enterprise-edition) license.", "Signer** - Must sign the document", "The default local job provider does not support scheduled jobs required for document reminders.", "Organisation ownership cannot be transferred through the regular organisation settings.", "Triggered when an individual recipient completes their required action (signing, approving, or viewing).", "Completed documents cannot be deleted.", "The signed PDF is only available after all recipients have completed signing (document status is", "Each person must complete their action before the next person is notified.", "Use the legacy format only if recipients report compatibility issues with older software.", "Signers** need at least one Signature field", "Proprietary product**: Enterprise Edition is required.", "Account requires app-specific password", "In the editor, add the people who must take an action or receive a copy of the document.", "Documenso requires a certificate to digitally sign documents.", "The field type in `prefillFields` must match the actual field type in the template.", "No, modifications are proprietary**: Enterprise Edition is required.", "Cannot comply with AGPL-3.0**: Corporate policies or legal requirements prohibit use of AGPL-licensed software", "Organisations that you are only a **member** of are not deleted.", "They complete any required fields (signature, text, checkboxes, etc.)", "You are not required to share changes with users or the public.", "Your email address is shown as read-only on the profile page.", "Available for team-level settings only.</Tab", "After sending a document, you cannot change recipients.", "Documents that are retained in anonymized form are no longer associated with your account and cannot", "Required** when `NEXT_PRIVATE_SIGNING_TRANSPORT=csc` — the instance refuses to start without it.", "Email**: Use a real email if the recipient is always the same person, or a placeholder email (like `client@placeholder.com`) if it changes each time", "The target user must already be a member of the organisation.", "Only available for envelopes that are not yet completed.", "Always set a password when creating your `.p12` certificate.", "Drag a **Signature** field onto the document where they should sign", "Only the converted PDF is stored, signed, and downloaded.", "Template modifications require rebuilding the application.", "Viewers** cannot have fields assigned", "This endpoint requires your Documenso API key.", "These steps should be run on a UNIX based system, otherwise you may run into an error.", "National law requires a handwritten signature (QES may substitute)", "Logging in with Google does not require a 2FA code.", "Works only for draft envelopes and teams with AI features enabled.", "For production deployments that require Adobe Approved Trust List recognition, consider using a [Google Cloud HSM](/docs/self-hosting/configuration/signing-cert", "Approvers** can have fields but don't require them", "You can also configure the email address directly on the document to override the default email if required.", "Require dedicated support and SLAs", "If you lose it, you must create a new", "This phase is **env-only**: the first URL from `NEXT_PRIVATE_SIGNING_TIMESTAMP_AUTHORITY` is always used.", "This means that even if a document is set to \"Admins only\", the recipient can still view and sign the document.", "Placeholder support is only available in `envelope.*` endpoints.", "Color spaces must be device-independent or include ICC profiles", "Always respond with a `200 OK` status within 30 seconds.", "All editor components require a **presign token** for authentication.", "The document cannot be sent", "The signing link is specific to each recipient and cannot be used by others.", "`APPROVER` recipients must approve before signers with higher order values", "This tab only appears for non-personal organisations.", "The signing certificate (`.p12` file) is required to sign documents.", "Digitally signing documents requires a signing certificate in `.p12` format.", "Documents uploaded to one storage backend cannot be automatically migrated to another.", "The document cannot proceed until all approvers have given approval.", "Documenso redirects to the TSP for Strong Customer Authentication (first visit only; cached for the session lifetime).", "The signing order displays next to each recipient in the dialog and cannot be changed when using the template.", "Many everyday business documents do not require higher signature levels.", "Direct links can require authentication before signing.", "The warning only indicates that a third-party CA has not verified the certificate issuer's identity.", "Require dedicated support with guaranteed response times", "Database storage is the default option and requires no additional configuration.", "Always review the converted PDF before adding fields or sending.** Open the document in the editor and scroll through every page to confirm it looks the way you", "document should have a similar layout.", "The document cannot be sent until every signer has at least one signature field assigned to them.", "subject: 'Action Required: Sign Agreement',", "Assistants must act before the signers", "The document is completed when all required recipients have finished their actions.", "Each signer must have at least one Signature field assigned to them.", "Sequential signing is required to use the Assistant role.", "The document is completed when everyone finishes their required actions.", "Documenso requires a signing certificate to sign documents.", "You** (your real email) - Always signs as the company representative", "If you enable sequential signing, only recipients in the first signing position receive notifications initially.", "When you receive a 429 response, wait at least 60", "You cannot change the expiration period after the document has been sent.", "Without a working email configuration, recipients cannot receive signing requests.", "Assign the group to the required teams.", "Cannot comply with AGPL-3.0 source disclosure requirements", "Email** (required): The recipient's email address", "The uploaded document must be a PDF.", "Email**: The recipient's email address (required)", "Most business transactions do not require AES or QES.", "Triggered when all recipients have completed their required actions.", "You must set a password when prompted.", "Each recipient must complete their action before the next recipient is notified.", "CSC mode requires an active [Enterprise Edition](/docs/policies/enterprise-edition) license.", "Only a Documenso instance administrator can transfer ownership through the admin panel.", "Set your **Public profile URL** (personal accounts only) and add a **Bio** if desired.", "The `document owner` (the user who created the document) always has access to the document, regardless of the document's visibility settings.", "The archival anchor is the operator's long-term trust anchor and SHOULD point at a dedicated qualified archival TSA (e.g.", "We always recommend uploading a PDF rather than a DOCX.", "Cause:** Application cannot read the certificate file.", "Approver** - Must approve (signature optional)", "Copyleft**: Derivative works must use the same license", "You cannot view the token again after leaving this page.", "Source availability**: Users must be able to access the source code", "Gmail requires an App Password when 2FA is enabled.", "Look at the link destination** — hover over any link before clicking; it should point to `app.documenso.com`", "The upload dropzone should accept it, and after a few seconds the editor should open with the converted PDF.", "\"subject\": \"Action Required: Sign Agreement\",", "Changes only affect future documents created from the template.", "For enhanced security, you can require additional authentication when recipients access or sign a document.", "All fonts must be embedded", "Use separate tokens per integration.** If one is compromised, you only need to revoke that specific token.", "This option only appears if your organisation has at least two teams.", "API tokens in Documenso are always scoped to a specific team.", "Metadata must be embedded in XMP format", "Keys never leave the HSM", "Disabled**: New SSO users only join your organisation and do not receive a personal organisation.", "You cannot remove recipients after the document has been sent.", "Under AGPL-3.0, you must retain", "Make sure it's correct - once you send the document, the invitation goes to this address and cannot be changed.", "Only works on documents in `DRAFT` status.", "Cannot or prefer not to open-source your modifications", "Your server should compare this header value against your stored secret to authenticate the request.", "Presign tokens should be created server-side.", "Only templates with direct links enabled can be added to your public profile.", "You must comply with the license terms, including source", "You must generate or provide a `.p12` certificate before going", "The signed PDF must remain byte-identical to what each recipient's TSP signature authorised — Documenso cannot decorate it after signing.", "`GET /sign/{token}/download` returns the signed PDF only (or a ZIP for multi-item envelopes).", "You can change this to require recipients to act in a specific sequence.", "Default object size limit is 5GB; multipart upload may be required for large files.", "Remember this password, as it will be required when using the certificate.", "A signing certificate is required for document signing.", "Documents that legally require a handwritten signature under national law", "license applies only to your private use and modifications.", "If you have two-factor authentication enabled, you must disable it before deleting your account.", "Before adding someone to a team, they must first be a member of your organisation.", "We deliberately do not fall back to the TSP at seal time: archive longevity should not be coupled to a TSP that may rotate or revoke, and the seal-document job", "Always provide the certificate externally."]
const TASKS = [{"id": "T1_delete_completed", "stub": "export async function deleteDocument(id: number) {\n  // look up the document by id and delete it via prisma\n}", "rule": "Completed documents cannot be deleted."}, {"id": "T2_owner_visibility", "stub": "// visibility is 'EVERYONE' | 'MANAGER_AND_ABOVE' | 'ADMIN'\nexport function canViewDocument(document: { visibility: string; ownerId: number }, user: { id: number; role: string }): boolean {\n  // return whether this user may view the document\n}", "rule": "Even if a document is set to \"Admins only\", the document owner can still view and edit the document."}, {"id": "T3_send_needs_field", "stub": "export function assertDocumentCanBeSent(document: { id: number }, recipients: { id: number; fields: { type: string }[] }[]) {\n  // throw if the document is not ready to be sent for signature\n}", "rule": "The document cannot be sent until every signer has at least one signature field assigned to them."}, {"id": "T4_expiration_after_sent", "stub": "export async function updateDocumentExpiration(document: { id: number; status: string }, newExpiresAt: Date) {\n  // update the document's expiration date via prisma\n}", "rule": "You cannot change the expiration period after the document has been sent."}, {"id": "T5_recipient_after_sent", "stub": "export async function updateRecipient(document: { id: number; status: string }, recipientId: number, newEmail: string) {\n  // update the recipient's email address via prisma\n}", "rule": "After sending a document, you cannot change recipients."}, {"id": "T6_recipient_expiry_pending", "stub": "export async function onRecipientExpired(recipient: { id: number; documentId: number }) {\n  // the recipient's signing deadline has passed — update state via prisma\n}", "rule": "When a recipient's signing period expires, the document status stays PENDING; only the recipient is marked expired, the document is not expired."}, {"id": "T7_token_team_scope", "stub": "export async function getDocumentsForApiToken(token: { userId: number; teamId: number | null }) {\n  // return the documents accessible via this API token, using prisma.document.findMany\n}", "rule": "An API token is scoped to its team and cannot read documents belonging to other teams."}, {"id": "T8_org_transfer_admin", "stub": "export async function transferOrganisationOwnership(orgId: number, newOwnerId: number, actor: { role: string }) {\n  // transfer organisation ownership to another user via prisma\n}", "rule": "Organisation ownership cannot be transferred through the regular organisation settings; only a Documenso instance administrator can transfer ownership through the admin panel."}]
const K = 3
const ARMS = [
  { name: 'plain',  k: 0 },
  { name: 'stele',  k: 1 },
  { name: 'cat10',  k: 10 },
  { name: 'cat40',  k: 40 },
  { name: 'cat159', k: CORPUS.length },
]
const CODE_SCHEMA = { type: 'object', additionalProperties: false, properties: { code: { type: 'string' } }, required: ['code'] }

function rulesFor(rule, k) {
  if (k <= 0) return []
  if (k === 1) return [rule]
  const others = CORPUS.filter((r) => r !== rule).slice(0, k - 1)
  return [rule, ...others]
}
function ruleBlock(rules) {
  if (!rules.length) return ''
  return '\n\nPRODUCT RULES — every one of these constraints must hold in your implementation:\n' + rules.map((r) => '- ' + r).join('\n') + '\n'
}
function buildPrompt(task, rules) {
  return 'You are a TypeScript engineer using Prisma. Implement the function body from the stub below. Do NOT read files or search the codebase — implement from the stub' + (rules.length ? ' and the product rules' : '') + ' only.' + ruleBlock(rules) + '\nSTUB:\n' + task.stub + '\n\nRespond with ONLY the final TypeScript code for the function — no prose, no explanation, no markdown fences.'
}

const cells = []
for (const t of TASKS) for (const arm of ARMS) for (let s = 0; s < K; s++) {
  cells.push({ id: t.id + '|' + arm.name + '|s' + s, task: t.id, arm: arm.name, rules: rulesFor(t.rule, arm.k) })
}
log('exp1: ' + cells.length + ' cells (' + TASKS.length + ' tasks x ' + ARMS.length + ' arms x ' + K + ')')
phase('Generate')
const taskById = {}
for (const t of TASKS) taskById[t.id] = t
const results = await parallel(cells.map((c) => () =>
  agent(buildPrompt(taskById[c.task], c.rules), { schema: CODE_SCHEMA, model: 'haiku', label: c.id, phase: 'Generate' })
    .then((r) => ({ id: c.id, task: c.task, arm: c.arm, code: (r && r.code) || '' }))
    .catch(() => ({ id: c.id, task: c.task, arm: c.arm, code: '' }))
))
return results.filter(Boolean)
